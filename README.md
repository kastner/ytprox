Initial setup
=============

```
brew install ffmpeg
brew install youtube-dl
brew install rbenv

rbenv install 2.3.5
export PATH=~/.rbenv/shims:$PATH

gem install bundler
bundle install
rbenv rehash

rackup
```
